Markdown Extra
==============

Set of extensions for ``python-markdown``.


Features
--------

  + YAML meta support
  + summary extraction


Installation
------------

Install from PyPI with ``pip``: ``pip install markdown-extra``.
